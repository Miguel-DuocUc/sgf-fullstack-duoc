services:
  oracle-db:
    image: gvenzl/oracle-xe:21-slim
    container_name: oracle-sgf
    ports:
      - "1521:1521"
    environment:
      ORACLE_PASSWORD: 1234
      APP_USER: MS_USERS
      APP_USER_PASSWORD: 1234
    volumes:
      - oracle-data:/opt/oracle/oradata

volumes:
  oracle-data: